name: ${project.artifactId}-bungee
main: ${project.groupId}.bungee.MinecraftAuthenticatorBungee
version: ${project.version}
author: xXSchrandXx
description: WSC-Minecraft-Authenticator
softdepend: []

commands:
  login:
    description: Command to log in.
  logout:
    description: Command to log out.
  wscauthenticatorbungee:
    description: Command for WSC-Minecraft-Authenticator.